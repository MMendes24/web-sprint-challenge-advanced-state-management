const initialState = {
    // local data
    isLoading: false,
    error: "",

    //from API
    data: "",
}

const smurfReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default smurfReducer